# 📌 My API

## 🚀 Requisitos

Antes de tudo, certifique-se de que o Python está instalado:

```bash
python --version
```

> Projeto desenvolvido em **Python 3.14.0**

---

## 🧱 Criar ambiente virtual

Crie um ambiente isolado para o projeto:

```bash
python -m venv venv
```

Ative o ambiente:

```bash
venv\Scripts\activate
```

---

## 📦 Instalar dependências

Instale todas as dependências necessárias:

```bash
pip install -r requirements.txt
```

---

## ▶️ Executar a API localmente

Inicie o projeto em modo local:

```bash
python -m src
```

---

## 📚 Documentação da API

Depois de rodar a aplicação, acesse a documentação interativa (Swagger):

👉 **[http://localhost:8000/docs](http://localhost:8000/docs)**

---

## 🔑 Chave da API

Auth Type: **API Key**

Add to: **Header**

#### **KEY:**
```bash
access_token
```
#### VALUE:
```bash
my-api-key-test
```
